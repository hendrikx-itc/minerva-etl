#!/bin/bash

IMAGE_NAME=python-test

docker build -t $IMAGE_NAME -f Dockerfile.python $WORKSPACE

docker run --rm -v $WORKSPACE:/workspace -w /workspace $IMAGE_NAME /workspace/docker-context/run-tests /workspace/tests
